import React from 'react';

import { LanguagesContainer, CurrentLanguage } from './styles';

const Languages = () => (
    <LanguagesContainer>
        <CurrentLanguage language="pt" />
    </LanguagesContainer>
);

export default Languages;
